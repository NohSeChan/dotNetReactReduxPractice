<?xml version="1.0" encoding="utf-8" ?>
<mapper>
	<select id="SelectBoardList">
		SELECT
			BOARDNO,
			BOARDTITLE,
			BOARDAUTHOR,
			BOARDVIEW
		FROM
			T_BOARD
		ORDER BY BOARDNO DESC
	</select>
	<insert id="InsertBoard">
		INSERT INTO T_BOARD(BOARDTITLE, BOARDAUTHOR, BOARDCONTENTS, CREATE_DATETIME)
		VALUES (@BOARDTITLE, @BOARDAUTHOR, @BOARDCONTENTS, GETDATE())
	</insert>
</mapper>