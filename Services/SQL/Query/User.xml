<?xml version="1.0" encoding="utf-8" ?>
<mapper>
	<select id="GetUserById">
		SELECT 
			ID, 
			PASSWORD,
			USER_NAME
		FROM 
			T_USER
		WHERE 
			ID = @id
	</select>
	
	<select id="GetUserByPw">
		SELECT 
			ID,
			PASSWORD,
			USER_NAME
		FROM
			T_USER
		WHERE
			ID = @id
			AND PASSWORD = @password
	</select>
</mapper>